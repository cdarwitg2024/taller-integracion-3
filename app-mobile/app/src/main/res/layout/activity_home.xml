<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 1. TU ESTRUCTURA ORIGINAL (Todo lo que está dentro del RelativeLayout) -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/bg_app">

        <!-- Toolbar / Barra superior para abrir el menú lateral -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:background="@color/bg_app"
            app:navigationIcon="@android:drawable/ic_menu_sort_by_size"
            app:title="CoffeeFaster"
            app:titleTextColor="@color/mocha_dark" />

        <!-- El contenedor donde se cargan los fragments -->
        <FrameLayout
            android:id="@+id/fragmentContainer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/toolbar"
            android:layout_alignParentBottom="true" />

    </RelativeLayout>

    <!-- 2. EL MENÚ LATERAL (Se superpone cuando lo deslizas) -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="260dp"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="#D2B49C"
        app:headerLayout="@layout/nav_header"
        app:itemTextColor="@color/mocha_dark"
        app:itemIconTint="@color/mocha_dark"
        app:menu="@menu/drawer_menu">

        <!-- Botón LogOut en la parte inferior del menú -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnDrawerLogout"
            android:layout_width="140dp"
            android:layout_height="44dp"
            android:layout_gravity="bottom|start"
            android:layout_margin="16dp"
            android:text="LogOut"
            android:textColor="@color/white_pure"
            android:textSize="14sp"
            android:textStyle="bold"
            app:backgroundTint="#A93226"
            app:cornerRadius="16dp" />
    </com.google.android.material.navigation.NavigationView>

</androidx.drawerlayout.widget.DrawerLayout>